<% content_for :title, "New Asset" %>

$(".modal").modal('hide');
var block = $("<%= j render(partial: "modal_form", layout: "layouts/railgun/modal") %>");
block.find("form").data({
  "remote": true
});
block.modal('show');
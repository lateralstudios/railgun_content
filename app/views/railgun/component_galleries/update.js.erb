var component = $(".block-component[role='<%= resource.component.id %>']");

<% if resource.valid? %>
  component.find(".box-content").html("<%= j render resource %>");
<% else %>
component.find(".box-content").html("<%= j render "form" %>");

Railgun.init.chosen(component.find('.chosen-select'));
<% end %>
